version: '3.8'
services:
  db:
    image: mysql:8.0
    container_name: mysql-server
    restart: always
    environment:
      # Use a password you'll remember
      MYSQL_ROOT_PASSWORD: Creation96_
      MYSQL_DATABASE: lab_db
      MYSQL_USER: bangz
      MYSQL_PASSWORD: Creation96_
    ports:
      - "3306:3306"
    volumes:
      - ./data:/var/lib/mysql
